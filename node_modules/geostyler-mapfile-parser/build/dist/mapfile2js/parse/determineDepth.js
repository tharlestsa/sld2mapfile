"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.determineDepth = void 0;
/**
 * Determines the depth of every line of mapfile.
 * @param {LineObject[]} lineObjects Array of line objects
 */
function determineDepth(lineObjects) {
    var depth = 0;
    lineObjects.forEach(function (lineObject) {
        lineObject.depth = depth;
        if (!lineObject.isBlockKey) {
            depth++;
        }
        else {
            if (lineObject.key) {
                if (lineObject.key.toUpperCase() === 'END') {
                    depth--;
                    lineObject.depth = depth;
                }
            }
        }
    });
    return lineObjects;
}
exports.determineDepth = determineDepth;
//# sourceMappingURL=determineDepth.js.map