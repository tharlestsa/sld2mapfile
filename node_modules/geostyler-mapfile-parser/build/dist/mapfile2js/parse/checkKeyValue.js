"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.checkKeyValue = void 0;
function removeQuotes(str) {
    if (/^['"].+['"]$/.test(str)) {
        return str.substring(1, str.length - 1);
    }
    return str;
}
/**
 *
 * @param {LineObject} lineObject Line without comment
 */
function checkKeyValue(lineObject) {
    if (lineObject.contentWithoutComment.match(/\s/)) {
        // Check key value
        var lineParts = lineObject.contentWithoutComment.split(/\s/);
        lineObject.key = removeQuotes(lineParts[0]).toLowerCase();
        var value = lineObject.contentWithoutComment.replace(lineParts[0], '').trim();
        // do not mess with expressions, quotes have meaning
        lineObject.value = lineObject.key.toUpperCase() === 'EXPRESSION' ? value : removeQuotes(value);
    }
    else {
        // key only
        lineObject.key = lineObject.contentWithoutComment.toLowerCase();
    }
    return lineObject;
}
exports.checkKeyValue = checkKeyValue;
//# sourceMappingURL=checkKeyValue.js.map