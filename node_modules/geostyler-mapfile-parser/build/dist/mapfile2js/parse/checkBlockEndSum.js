"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.checkBlockEndSum = void 0;
/**
 *
 * @param {LineObject[]} lines Array of line objects
 */
function checkBlockEndSum(lines) {
    var blockCounter = 0;
    var endCounter = 0;
    lines.forEach(function (line) {
        if (line.isBlockLine) {
            blockCounter++;
            endCounter++;
        }
        else {
            if (line.isBlockKey) {
                blockCounter++;
            }
            else {
                if (line.key) {
                    if (line.key.toUpperCase() === 'END') {
                        endCounter++;
                    }
                }
            }
        }
    });
    if (blockCounter !== endCounter) {
        if (blockCounter < endCounter) {
            throw new Error("Too many ends (blocks: " + blockCounter + ", ends: " + endCounter + ").");
        }
        else {
            throw new Error("Too few ends (blocks: " + blockCounter + ", ends: " + endCounter + ").");
        }
    }
}
exports.checkBlockEndSum = checkBlockEndSum;
//# sourceMappingURL=checkBlockEndSum.js.map