"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.UndoUtil = void 0;
/**
 * Helper class for state/undo-redo.
 *
 * @class
 */
var UndoUtil = /** @class */ (function () {
    function UndoUtil() {
    }
    /**
     * Checks if at least one state is undoable or not.
     *
     * @param {Object} state The global state.
     * @return {boolean} Whether at least one state is undoable or not.
     */
    UndoUtil.atLeastOneUndoable = function (state) {
        for (var _i = 0, _a = Object.entries(state); _i < _a.length; _i++) {
            var _b = _a[_i], k = _b[0], v = _b[1];
            if (k && v && v.past && v.past.length > 0) {
                return true;
            }
        }
        return false;
    };
    /**
     * Checks if at least one state is redoable or not.
     *
     * @param {Object} state The global state.
     * @return {boolean} Whether at least one state is redoable or not.
     */
    UndoUtil.atLeastOneRedoable = function (state) {
        for (var _i = 0, _a = Object.entries(state); _i < _a.length; _i++) {
            var _b = _a[_i], k = _b[0], v = _b[1];
            if (k && v && v.future && v.future.length > 0) {
                return true;
            }
        }
        return false;
    };
    return UndoUtil;
}());
exports.UndoUtil = UndoUtil;
exports.default = UndoUtil;
